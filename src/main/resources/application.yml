spring:
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
user:
  check:
    enabled: false
  service:
    protocol: http
    host: localhost
    port: 8085